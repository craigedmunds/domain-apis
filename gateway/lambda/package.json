{
  "name": "domain-api-gateway-lambda",
  "version": "1.0.0",
  "description": "Aggregation Lambda for Domain API Gateway",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && zip -r aggregation-lambda.zip dist/ node_modules/",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.3",
    "js-yaml": "^4.1.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/jest": "^29.5.11",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "@types/node-fetch": "^2.6.9",
    "fast-check": "^4.5.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
