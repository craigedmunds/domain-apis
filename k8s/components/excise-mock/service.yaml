apiVersion: v1
kind: Service
metadata:
  name: excise-mock
  labels:
    app.kubernetes.io/name: excise-mock
    app.kubernetes.io/component: mock
spec:
  selector:
    app.kubernetes.io/name: excise-mock
  ports:
    - name: http
      port: 8080
      targetPort: 8080
